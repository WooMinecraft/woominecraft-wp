language: php
php:
  - '7.1'
  - '7.2'
env:
  - RELEASE_PREFIX="woominecraft-prerelease-b"
before_script:
  - phpenv config-rm xdebug.ini
cache:
  directories:
    - vendor
jobs:
  include:
    - stage: phplint
      if: |
        branch = main OR \
        branch =~ /^release\/.*$/
      script:
        - composer install
        - composer run-script phpcs